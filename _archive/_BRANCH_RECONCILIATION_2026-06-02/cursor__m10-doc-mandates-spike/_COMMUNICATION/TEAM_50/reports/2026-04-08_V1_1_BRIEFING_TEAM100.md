---
document_type: TEAM_BRIEFING
version: "1.0"
---

# Team 50 — Briefing: v1.1.0 Clarifications Resolved + QA Mandate v1.1

**From:** Team 100 (Architecture)  
**To:** Team 50 (QA)  
**Date:** 2026-04-08  
**Subject:** All 8 information requests resolved — QA_MANDATE_G_V1_1.md updated to v1.1  

---

## 0. Test impact at a glance (G-V1.1)

| Test | Priority | Gate if FAIL? | Primary command / evidence | Completion report item (briefing §3) |
|------|----------|---------------|----------------------------|--------------------------------------|
| T01 | CRITICAL | Yes | `pytest tests/ -m "not upress"` | — (pytest log in §13) |
| T02 | CRITICAL | Yes | SQL distinct unresolvable ≤ 20 | Metrics / triage context |
| T03 | HIGH | 3+ HIGH → FAIL | SQL SRC021 unresolvable ≤ 10 | A2 triage |
| T04 | CRITICAL | Yes | `len(products)` in `public_report.json` ≥ 77 | Phase B |
| T05 | HIGH | 3+ HIGH | PRD027 count ≤ 1 in published JSON | Item 6 |
| T06 | CRITICAL | Yes | SQL cherry aliases on PRD001 = 0 | A1 |
| T07 | CRITICAL | Yes | SQL active aliases PRD028/PRD029 = 0 | A1 |
| T08 | HIGH | Matrix | Completion report C1 egg matrix | Item 3 |
| T09 | HIGH | Matrix | Completion report C2 passion fruit | Item 4 |
| T10 | MEDIUM | Doc | Completion report C3 blueberries | Item 5 |
| T11 | HIGH | Functional | Tier SQL + `pytest test_basket_tier_resolver` | Item 7 |
| T12 | MEDIUM | Doc | ADR path or C3 notification | Item 8 |
| T13 | HIGH | Doc | `WHATSAPP_DATA_SUBMISSION_PROTOCOL.md` + `pending_manual` / 073 | A4 |
| T14 | MEDIUM | Functional | Blog placeholder + vision link | A4 |
| T15 | CRITICAL | **Privacy override** | Grep JSON/HTML for SRC / PII | Item 9 |
| T16 | CRITICAL | Yes | Full pipeline success; witness or re-run | Items 10–11 |

---

## 1. Status: Your information requests are fully resolved

All 8 items from your request (`QA-INFO-REQ-20260408-V1-1-LOD400`) have been answered in the canonical ARCH_DECISION:

> **ARCH-20260408-TEAM50-CLARIFICATIONS-V1-1**  
> `_COMMUNICATION/TEAM_100/reports/2026-04-08_TEAM50_CLARIFICATIONS_RESPONSE_TEAM100.md`

Attach that document path to your future `QA_REVIEW_REQUEST` as the clarifications record. This briefing highlights what changed and what you need to verify.

---

## 2. What changed in the documents you will use

### `QA_MANDATE_G_V1_1.md` — updated to v1.1 (2026-04-08)

| Change | Details |
|--------|---------|
| **Scope statement** | Now explicitly references: `SPEC-20260408-PHASE-A-LOD400` (primary binding), `MANDATE-20260408-V1-1-LOD400-EXEC` (execution order), `HANDOFF-20260408-V1-1-ORCH-TEAM10` (coordination). LOD400 governs over consolidated mandate at implementation detail. |
| **T05 criterion** | Changed from "Exactly 1 row for PRD027" → **≤ 1 (0 = below publish threshold: PASS with note; 1 = confirmed: PASS; 2+ = FAIL)** |
| **T12 Pantry ADR** | Clarified: ADR is authored by **Team 100** (not Team 10). If ADR not yet filed, verify C3 notification report from Team 10 exists — this is an intermediate PASS condition. |

### `MANDATE_V1_1_LOD400_EXEC_TEAM10.md` — amended (affects T08–T12 evidence)

| Change | Your impact |
|--------|------------|
| **Phase C task mapping corrected** | C2 = Passion Fruit/PRD072 (not basket alias), C3 = Blueberries/PRD086 (not Pantry ADR), C4 = basket_tier_resolver. Your T08/T09/T10 now have correct completion-report counterparts. |
| **Phase D: "Unit Summary Report" removed** | That task had no spec authority. Team 10 completion report item was never "unit summary report." Phase D = C3 notification to Team 100 only. |
| **Completion report now has 13 required items** | Items 4 (C2 passion fruit matrix) and 5 (C3 blueberries table) added. Item 8 now correctly says "D1 ADR cross-reference (Team 100 authored)." |
| **CLI commands corrected** | Phase B canonical: `python -m organic_market_agent run_ingestion --run-type manual --normalize` (no `--all-sources`). Phase E adds `--upload`. |

---

## 3. Authoritative Phase C/D mapping — what T08–T12 are checking

| Test | Checks for | Completion report item |
|------|-----------|----------------------|
| **T08** | Eggs (PRD067) source × unit matrix | §Phase C item 3 |
| **T09** | Passion fruit (PRD072) source × unit matrix | §Phase C item 4 |
| **T10** | Blueberries (PRD086) pack research table | §Phase C item 5 |
| **T11** | basket_tier_resolver — CSA tier assignment | §Phase C item 7 (test output) |
| **T12** | Pantry ADR at `_COMMUNICATION/TEAM_100/reports/2026-04-08_ADR_PACK_WEIGHT_COMPARISON_TEAM100.md` | §Phase D item 8 (Team 100-authored) |

There is **no T** for "Basket Alias Remapping" or "Unit Summary Report" — these tasks did not exist in the spec.

---

## 4. Canonical `basket_tier_resolver` API — for T11 assertion

```python
def resolve_basket_tier(
    csa_context_json: Optional[str],   # JSON string — NOT a dict
    price_amount: Optional[Decimal],   # Decimal — NEVER float
    session: Session,
) -> tuple[Optional[str], str]:        # (product_code, resolution_note)
```

Valid `product_code` values: `'PRD025'` | `'PRD026'` | `'PRD027'` | `None`  
Valid `resolution_note` values: `basket_tier_by_item_count` | `basket_tier_by_price` | `basket_tier_default_medium` | `basket_too_small` | `basket_tier_oversized_default_large`

Test assertion: code `!= None` → product exists in DB as `is_basket_product = true`. Tests must use `Decimal("120")` not `120.0`.

---

## 5. Canonical CLI blocks — for T15/T16 and your own verification

```bash
# Phase B (ingestion + normalize + publish without upload):
python -m organic_market_agent run_ingestion --run-type manual --normalize
python -m organic_market_agent run_aggregator
python -m organic_market_agent run_publisher

# Phase E (final with FTPS upload):
python -m organic_market_agent run_ingestion --run-type manual --normalize
python -m organic_market_agent run_aggregator
python -m organic_market_agent run_publisher --upload
```

`--all-sources` flag does not exist. Omitting `--source-code` runs all active sources.

---

## 6. New schema fact for T13 (`pending_manual` status)

Migration 073 will add `pending_manual` to the `chk_rei_extraction_status` CHECK constraint on `raw_extracted_items`. After that migration is applied:
- Valid extraction_status values: `extracted` | `normalized` | `unresolvable` | `ignored` | **`pending_manual`** (new)
- T13 WhatsApp protocol: verify that the protocol document references `pending_manual` and that migration 073 is applied (`alembic current` shows head ≥ 073)

---

## 7. A4 SQL column correction — for T13 evidence review

When reviewing Team 10's A4 evidence, the correct column names are:

| Wrong (original spec errata) | Correct (live schema) |
|-----------------------------|-----------------------|
| `raw_name` | `raw_product_name` |
| `raw_price` | `raw_price_text` (VARCHAR) |
| `raw_unit` | `raw_unit_text` |
| `s.source_code` | `s.code` |

The `raw_extracted_items` table has no direct `source_id` column — source is accessed via `source_fetch_run_id → source_fetch_runs.source_id`.

---

## 8. Information Team 10 must preload in their QA request

When Team 10 files the `QA_REVIEW_REQUEST`, it must include:

| Item | Needed for test |
|------|----------------|
| Before/after `catalog_scan_collect_metrics.py` JSON paths | T01/T02 |
| A2 triage table (all 92 names classified) | T02/T03 |
| Phase B ingestion run row + source success/fail table | T16 |
| C1 egg unit matrix | T08 |
| C2 passion fruit unit matrix | T09 |
| C3 blueberries research table | T10 |
| D1 Pantry ADR path (Team 100-authored) or C3 notification report path if ADR pending | T12 |
| WhatsApp protocol document path + blog placeholder status | T13/T14 |
| Team 190 preflight PASS report path | Precondition (gate §5) |

**Do not begin G-V1.1 execution until all items above are present in the Team 10 QA request.**

---

## 9. Your next action

**Hold.** Your QA mandate is now accurate (v1.1). Your clarifications are answered. You have everything you need to execute G-V1.1 when the package arrives.

**Next trigger:** Wait for Team 10 completion report + Team 190 PASS → Team 10 files `QA_REVIEW_REQUEST` → execute `QA_MANDATE_G_V1_1.md`.

---

*From: Team 100 (Architecture) — 2026-04-08*
