# LIVE RE-QA (R2) — SFA-S003-P004-WP-CB-UI-ALIGN — team_50 — v1.0.0

**Date:** 2026-06-02 · **By:** team_50 (orchestrated by team_100) · **Target:** LIVE `https://sfa.nimrod.bio`
**Deployed:** from `origin/main` (team_99 DEPLOY_REPORT v1.0.2; live includes V01+V02+V03). **Re-QA of the 3
L-GATE_V R1 findings + no-regression.**

## Findings re-verified on live

| Finding | R1 | R2 result | Live evidence |
|---|---|---|---|
| **V01** `/calc/export.pdf` 404 | BLOCKER | ✅ **RESOLVED** | PDF moved to extension-less `/calc/print`: `GET /calc/print`→200 text/html, `/calc/print?crop=test`→200; `/calc/export.csv`→200; old `/calc/export.pdf`→404 (route retired, expected). |
| **V02** crop pages leak raw enum/field keys | MAJOR | ✅ **RESOLVED** | `/crop-book/lettuce` raw-enum scan (`direct_seed`/`half_hardy`/`family:variety`/`>yield_per_bed_m<`/`>price_documented<`) → **0 hits**. (team_99 R2 also verified `/crop-book/watermelon`; `yield_per_bed_m` remains only inside `data-field="…"` attributes, visible text Hebrew.) |
| **V03** empty calc crop `<select>` | MAJOR | ✅ **RESOLVED** | `/calc/` → **70 real crop `<option>`s** (Hebrew labels) + the placeholder; `SFA_CROP_BOOK` + `applyBookValues()` bind book values on select. |

## No-regression (live)
Baseline `/`, `/crop-book/`, `/calc/`, `/market/` → all 200 (team_99 R2 baseline + team_100 spot). AC-1 (#f8fbf8,
no cream), AC-2 (`.sh` shell + `#sfa-logo` site-wide, no legacy chrome) confirmed PASS in R1 and unchanged.

## Verdict
**PASS (internal, live).** All three L-GATE_V R1 findings are resolved and verified on the live deployment.
Ready for team_190 **L-GATE_V Round 2** (Cursor) — the constitutional per-page round on live, including the
AC-3 design-vs-live screenshot pairs for crop simple/full/drill + calc-dash.

## Note — repo hygiene (team_191)
The V01 fix commits (fdb4fc7/fc4b914) landed on **origin/main**; the feature branch
`claude/wp-cb-ui-align-2026-06-02` diverged (tip e8c7e37, Class B). team_99 deployed from main (superset). A
concurrent Class B session also has uncommitted work in the shared tree. Recommend team_191 reconcile
(ff-merge main→branch or retire the branch at WP closure). Live + main are the source of truth.

---
*Method: curl battery vs https://sfa.nimrod.bio (route status, raw-enum scan on a real crop page, selector option
count) + team_99 DEPLOY_REPORT v1.0.1/v1.0.2 evidence.*
