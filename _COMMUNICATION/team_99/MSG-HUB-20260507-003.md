---
id: MSG-HUB-20260507-003
schema_version: aos_v1_team_messaging
from_team: team_100
to_team: team_99
type: task
subject: "Deploy WP007 (WP REST API upload) + Smoke + WP003 Pass-2 — F-01 actual fix"
date: 2026-05-07T23:30:00Z
related_wp: SFA-S002-P001-WP007
expects_response: true
status: SENT
next_step: "Await team_00 to supply WP Application Password (3 env vars). Then add to /data/projects/smallfarmsagents/.env, install JSON-MIME mu-plugin, deploy, smoke, run WP003 Pass-2."
handoff_to: team_99
handoff_context_pointer: _COMMUNICATION/team_10/SFA-S002-P001-WP007/DEPLOY_HANDOFF.md
project_id: smallfarmsagents
---

## Deploy WP007 + Smoke + WP003 Pass-2

team_99 — sfa_build (Sonnet) completed WP007 build (commit `73eaf3e`). This is the **actual fix for F-01**, replacing FTPS port 21 (Bezeq-blocked) with WP REST API on port 443 (proven working — same pattern as shaked-wg-agent on this server).

### State

```
branch:        offline/2026-05-07-smallfarmsagents-release-prep
build commit:  73eaf3e
roadmap commit: 237da3f
remote:        origin (pushed)
spoke:         /data/projects/smallfarmsagents/   (waldhomeserver)
```

### One blocking dependency before you start

**team_00 must supply 3 env values for `/data/projects/smallfarmsagents/.env`:**
- `UPRESS_WP_REST_BASE` (likely `https://www.nimrod.bio/wp-json` — same as shaked-wg)
- `UPRESS_WP_APP_USER` (WordPress user)
- `UPRESS_WP_APP_PASS` (WordPress application password)

Once team_00 provides these, follow `DEPLOY_HANDOFF.md` step-by-step.

### Read order

1. **MSG (this file)**
2. **DEPLOY_HANDOFF (binding step-by-step):**
   `_COMMUNICATION/team_10/SFA-S002-P001-WP007/DEPLOY_HANDOFF.md`
3. **WP007 LOD400 (AC source):**
   `_aos/work_packages/S002/SFA-S002-P001-WP007/LOD400_spec.md`
4. **Architectural decision (Option A — manifest pointer):**
   `_COMMUNICATION/team_10/SFA-S002-P001-WP007/SHORTCODE_INTEGRATION_DECISION.md`
5. **WP003 LOD400 (Pass-2 AC matrix):**
   `_aos/work_packages/S002/SFA-S002-P001-WP003/LOD400_spec.md`

### What you will do

1. **Pull offline branch:**
   ```bash
   cd /data/projects/smallfarmsagents
   git fetch origin
   git checkout offline/2026-05-07-smallfarmsagents-release-prep
   git pull
   ```

2. **Update `.env`** with the 3 lines team_00 provided.

3. **Pre-emptively install JSON-MIME mu-plugin** at uPress (PHP snippet in `DEPLOY_HANDOFF.md` §3) — required because WP media library may reject `.json` uploads by default.

4. **Add `register_setting` PHP** to `functions.php` per DEPLOY_HANDOFF §4, then re-run `python scripts/wp_shortcode_install.py` (without `--set-mou-url` first).

5. **Run upload:**
   ```bash
   python -m organic_market_agent run_publisher --upload
   ```
   Expected: 4 artifacts uploaded + 1 `sfagent-manifest-of-urls.json` pointer file. Console prints the pointer URL.

6. **Wire the pointer URL into the shortcode option:**
   ```bash
   python scripts/wp_shortcode_install.py --set-mou-url <pointer-url-from-step-5>
   ```

7. **Smoke verification:**
   - Curl `https://www.nimrod.bio/SmallFarmsAgent` → HTTP 200
   - Confirm rendered page shows fresh `artifact_version` (today's run, NOT `20260417_004822`)
   - Confirm `staleness_level == fresh` and `report_date` is current (NOT `2099-08-12`)

8. **WP003 Pass-2 verification report:**
   - Re-run AC-01..AC-07 from `_aos/work_packages/S002/SFA-S002-P001-WP003/LOD400_spec.md`
   - File: `_COMMUNICATION/team_99/SFA-S002-P001-WP003/VERIFICATION_REPORT_PASS2_v1.0.0.md` with `pass: 2` + verdict
   - Cross-deliver copy to `_COMMUNICATION/TEAM_60/reports/`

9. **Push** to `origin/main` per your contract Push Authority.

### Hard constraints

- **No application code changes** (your IR §63). If smoke fails, surface findings to team_100 — do NOT code-fix.
- No `_aos/` writes (your IR#13).
- Secrets: WP App Password lives ONLY in `/data/projects/smallfarmsagents/.env`, NEVER in artifacts/commits.
- If JSON MIME issue surfaces despite mu-plugin, log + escalate to team_100.

### Self-attest L-GATE_BUILD

OPS-track self-attest per ADR044 §1. Pass-2 completes when AC-04 (was FAIL) lifts to PASS plus all other AC are PASS. Once your report lands, team_100 closes WP003 + WP007.

---

MSG: MSG-HUB-20260507-003 | מ: team_100
פעולה: env update → mu-plugin → register_setting → publisher --upload → set-mou-url → smoke → WP003 Pass-2 report → push
