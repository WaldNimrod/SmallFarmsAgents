---
id: MSG-HUB-20260507-004
schema_version: aos_v1_team_messaging
from_team: team_100
to_team: team_99
type: status
subject: "Unblock — WP REST creds in place; proceed with WP007 deploy per MSG-003"
date: 2026-05-07T23:55:00Z
related_wp: SFA-S002-P001-WP007
expects_response: true
status: SENT
next_step: "Resume MSG-003 step 3 onward (mu-plugin → register_setting → run_publisher --upload → set-mou-url → smoke → WP003 Pass-2)."
handoff_to: team_99
handoff_context_pointer: _COMMUNICATION/team_99/MSG-HUB-20260507-003.md
project_id: smallfarmsagents
---

## Unblock — WP REST creds in place

The blocker from MSG-HUB-20260507-003 (env vars supplied by team_00) is **resolved**.

team_100 verified end-to-end:

```
Mac /Users/nimrod/Documents/SmallFarmsAgents/.env       — 3 keys SET (auth → HTTP 200 from Mac)
server /data/projects/smallfarmsagents/.env             — 3 keys SET (auth → HTTP 200 from server)
server /data/projects/shaked-wg-agent/.env              — 3 keys SET (sibling sync, env_from_sfa.sh pattern)

UPRESS_WP_APP_USER = agent  (WP user id 373, slug 'agent')
UPRESS_WP_APP_PASS = <29 chars, 5 spaces — canonical WP application password format>
UPRESS_WP_REST_BASE = https://www.nimrod.bio/wp-json
```

Server-side auth probe results (just now):
- `GET /wp/v2/users/me` → HTTP 200
- `GET /wp/v2/media?per_page=1` → HTTP 200

The label `sfa-pipeline` (the application password's name in WP Admin) is for human readability; the username for Basic auth is `agent`.

## What you do next

Resume MSG-HUB-20260507-003 from step 3 onward:
1. ~~Get creds from team_00~~ — done.
2. ~~Add 3 env vars~~ — done.
3. **Pre-emptively install JSON-MIME mu-plugin** at uPress (PHP snippet in `_COMMUNICATION/team_10/SFA-S002-P001-WP007/DEPLOY_HANDOFF.md` §3).
4. Add `register_setting` PHP per DEPLOY_HANDOFF §4, run `python scripts/wp_shortcode_install.py` (no `--set-mou-url` yet).
5. Run upload: `cd /data/projects/smallfarmsagents && python -m organic_market_agent run_publisher --upload`
6. Wire pointer URL into shortcode: `python scripts/wp_shortcode_install.py --set-mou-url <printed-pointer-url>`
7. Smoke verify: site shows fresh `artifact_version`, `staleness_level=fresh`, current `report_date`.
8. WP003 Pass-2 report → push.

## Backups taken

```
/data/projects/smallfarmsagents/.env.bak.20260507_wp007_creds
/data/projects/shaked-wg-agent/.env.bak.20260507_wp007_creds
```

Restore via `mv ${file}.bak.20260507_wp007_creds ${file%.bak*}` if anything goes wrong.

---

MSG: MSG-HUB-20260507-004 | מ: team_100
פעולה: resume MSG-003 step 3 onward; reply when smoke + WP003 Pass-2 complete.
