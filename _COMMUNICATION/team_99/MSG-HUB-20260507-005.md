---
id: MSG-HUB-20260507-005
schema_version: aos_v1_team_messaging
from_team: team_100
to_team: team_99
type: status
subject: "Unblock — mu-plugin installed, JSON+HTML smoke verified; resume MSG-003 step 5"
date: 2026-05-07T02:35:00Z
related_wp: SFA-S002-P001-WP007
expects_response: true
status: SENT
next_step: "Resume MSG-003 step 5: run_publisher --upload, set-mou-url, smoke, WP003 Pass-2."
handoff_to: team_99
handoff_context_pointer: _COMMUNICATION/team_99/MSG-HUB-20260507-003.md
project_id: smallfarmsagents
---

## Unblock — mu-plugin installed + verified

team_00 manually installed `sfagent-allow-json.php` at `wp-content/mu-plugins/` via uPress panel file manager.

team_100 ran a smoke test from `waldhomeserver` (`/data/projects/smallfarmsagents/.env` creds):

```
POST /wp/v2/media — sfa-smoke-test.json  → HTTP 201 (id 91377)
POST /wp/v2/media — sfa-smoke-test.html  → HTTP 201 (id 91378)
DELETE both test ids                     → HTTP 200 (cleanup OK)
```

The MIME restriction is fully bypassed. JSON + HTML uploads now work end-to-end.

## Resume order (from MSG-003)

You are at step 5. Continue:

1. ~~Pull offline branch~~ — done.
2. ~~Add 3 env vars~~ — done.
3. ~~Install JSON-MIME mu-plugin~~ — done (manually by team_00; verified by team_100).
4. ~~`register_setting` PHP~~ — confirm done; if not, add per DEPLOY_HANDOFF §4 then run `python scripts/wp_shortcode_install.py` (no `--set-mou-url` yet).
5. **Run upload:**
   ```bash
   cd /data/projects/smallfarmsagents
   python -m organic_market_agent run_publisher --upload
   ```
   Expected: 4 artifacts uploaded + 1 `sfagent-manifest-of-urls.json` pointer file. Console prints the pointer URL.

6. **Wire pointer URL:**
   ```bash
   python scripts/wp_shortcode_install.py --set-mou-url <pointer-url-from-step-5>
   ```

7. **Smoke verification:**
   - Curl `https://www.nimrod.bio/SmallFarmsAgent` → HTTP 200
   - Confirm rendered page shows fresh `artifact_version` (today's run, NOT `20260417_004822`)
   - Confirm `staleness_level == fresh` and `report_date` is current (NOT `2099-08-12`)

8. **WP003 Pass-2 verification report:**
   - Re-run AC-01..AC-07 from `_aos/work_packages/S002/SFA-S002-P001-WP003/LOD400_spec.md`
   - File: `_COMMUNICATION/team_99/SFA-S002-P001-WP003/VERIFICATION_REPORT_PASS2_v1.0.0.md` with `pass: 2`
   - Cross-deliver to `_COMMUNICATION/TEAM_60/reports/`

9. **Push** to `origin/main` per Push Authority.

## After Pass-2

Report verdict to `_COMMUNICATION/team_100/MSG-HUB-20260507-005-RESPONSE.md`. team_100 will then close WP006 + WP007, advance WP003 to L-GATE_V, and assemble WP005 bundle.

---

MSG: MSG-HUB-20260507-005 | מ: team_100
פעולה: resume from MSG-003 step 5 (env already set, mu-plugin installed, smoke verified at REST layer).
