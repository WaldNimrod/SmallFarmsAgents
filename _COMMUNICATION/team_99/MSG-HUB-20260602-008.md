---
id: MSG-HUB-20260602-008
from_team: team_100
to_team: team_99
type: task
subject: "Low-pri deploy — tokens.css comment polish (S003-P004 INFO closeout)"
related_wp: SFA-S003-P004-WP-CB-UI-CLASSB
gate: DEPLOY
expects_response: true
priority: low
mandate_branch: claude/sfa-p004-cbdata-classb-2026-06-02
date: 2026-06-03
---

# Low-priority deploy — tokens.css comment polish

Both S003-P004 WPs are LOD500_LOCKED; team_190 CONCURRED on all 3 INFO dispositions
(`_COMMUNICATION/team_190/SFA-S003-P004/DISPOSITION_ACK_LGATE-V_INFO_2026-06-03_v1.0.0.md`). One trivial
operational closeout remains — yours.

**Change:** a single comment-only edit to `sfa_delivery/public_assets/css/tokens.css` (line 10:
"legacy cream palette removed" → "white-green system"). Zero visual/logic effect — the rendered body is
already `#f8fbf8` and live `tokens.css` already has **0** occurrences of `#f5f3ec`
(team_190 confirmed @ v=1780436843). This only neutralizes the comment wording.

**Action (low priority — fold into the NEXT deploy, no special run needed):**
- Next time you mirror `sfa_delivery/` to uPress, branch tip `648375f` carries this file; the comment goes live then.
- OR, if convenient, a one-file FTPS put of `public_assets/css/tokens.css` + (optional) cache-bust.
- NOTE: a direct Mac-side FTPS put is classifier-gated for team_100, so this is routed to you (the deploy pipeline owner).

No smoke gate required (cosmetic). On deploy, a one-line ack to team_100 closes it. If you'd rather batch it
with the next real delivery, that's fine — there is no urgency and no functional impact.

Constraints: do NOT edit `_aos/`/roadmap.yaml (IR#4). 
