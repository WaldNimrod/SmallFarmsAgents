---
id: MSG-HUB-20260507-006
schema_version: aos_v1_team_messaging
from_team: team_100
to_team: team_99
type: task
subject: "Deploy WP008 + smoke scheduler+admin upload paths (F-190-01 fix) — P0 before 06:00 UTC cron"
date: 2026-05-07T03:50:00Z
related_wp: SFA-S002-P001-WP008
expects_response: true
status: SENT
next_step: "Pull main, restart sfa-pipeline service, smoke Admin 'Upload Now' + force scheduler run, confirm WP REST used (not FTPS)."
handoff_to: team_99
handoff_context_pointer: _COMMUNICATION/team_100/SFA-S002-P001-WP008/MANDATE_v1.0.0.md
project_id: smallfarmsagents
---

## Deploy WP008 — F-190-01 fix — P0 before 06:00 UTC cron

team_190's verdict (`ccb5939`) flagged F-190-01: scheduler `pipeline.py` + admin `runs_upload_now` still FTPS-only. Today's site is fresh because we ran `run_publisher --upload` manually (CLI path through WP REST). **The 06:00 UTC cron will fail tomorrow** without WP008.

WP008 build is complete (`06eca4b`) and merged to main (`53af3eb`). Mechanical refactor: `dispatch_upload()` shared helper, called from CLI + scheduler + admin. 20/20 tests pass. validate_aos 0 FAIL. No env changes required.

### State

```
branch:        main (offline already merged)
build commit:  06eca4b
merge commit:  53af3eb
spoke:         /data/projects/smallfarmsagents/   (waldhomeserver)
```

### What you do

1. **Pull main on waldhomeserver:**
   ```bash
   cd /data/projects/smallfarmsagents
   git fetch origin
   git checkout main
   git pull
   ```

2. **Restart the scheduler service:**
   ```bash
   sudo systemctl restart sfa-pipeline.service
   sudo systemctl status sfa-pipeline.service --no-pager
   ```
   Confirm `Active: active (running)`.

3. **Verify config is upload-capable:**
   ```bash
   cd /data/projects/smallfarmsagents
   .venv/bin/python -c "from organic_market_agent.utils.config import config; print('upress_configured:', config.upress_configured())"
   ```
   Expected: `upress_configured: True`.

4. **Smoke 1 — Admin UI button:** Open Admin UI in browser → click "Upload Now" → confirm flash/JSON response says `WP REST` (or contains the new helper's success indicator). `artifact_version` should advance.

5. **Smoke 2 — force scheduler run:**
   ```bash
   sudo systemctl start sfa-pipeline.service  # one-shot trigger
   sudo journalctl -u sfa-pipeline.service -n 50 --no-pager | grep -E "dispatch_upload|WP REST|FTPS|upload"
   ```
   Look for `dispatch_upload: WP REST upload OK` (or similar — the helper logs the protocol used). Should NOT see `ftps_upload` paths firing.

6. **Smoke 3 — public manifest:**
   ```bash
   curl -s https://www.nimrod.bio/wp-content/uploads/$(date +%Y/%m)/sfagent-manifest.json | head -c 500
   ```
   `artifact_version` should match the latest run's timestamp.

7. **File DEPLOY_LOG:**
   `_COMMUNICATION/team_99/SFA-S002-P001-WP008/DEPLOY_LOG_v1.0.0.md` with:
   - Pre-deploy state (current scheduler service status)
   - Post-deploy state (smoke results)
   - Verdict: PASS / PASS_WITH_FINDINGS / FAIL
   - Cross-deliver to `_COMMUNICATION/TEAM_60/reports/` if useful

8. **Push** to `origin/main` per Push Authority.

### Hard constraints

- No application code changes (your IR §63). If smoke fails, surface to team_100 — do NOT code-fix.
- No env changes needed (WP008 uses existing `UPRESS_WP_APP_*` already on server).
- Secrets discipline: no credentials in artifacts/logs.
- `UPRESS_FALLBACK_FTPS` should remain UNSET (default — WP REST primary, no FTPS attempt).

### Self-attest L-GATE_BUILD

OPS-track. Once smoke 1+2+3 PASS and DEPLOY_LOG filed → WP008 closes. team_100 closes Phase 1 in roadmap, runs ADR042 archival.

### Failure mode plan (if smoke FAILS)

- If WP REST fails on Admin button → check `/data/projects/smallfarmsagents/.env` has 3 `UPRESS_WP_*` keys (auth smoke from runbook §3).
- If scheduler logs show `ftps_upload` instead of `dispatch_upload` → service restart didn't pick up new code; try `systemctl daemon-reload && systemctl restart sfa-pipeline.service`.
- Fundamental code issue → surface to team_100, do NOT roll back without authorization.

---

MSG: MSG-HUB-20260507-006 | מ: team_100
פעולה: pull → restart → smoke 1+2+3 → DEPLOY_LOG → push. P0 before 06:00 UTC cron.
