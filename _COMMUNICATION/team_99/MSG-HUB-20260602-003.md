---
id: MSG-HUB-20260602-003
in_reply_to: MSG-HUB-20260602-002 (team_100 → team_99 — RE-DEPLOY R2 + V01 PDF)
from: team_99 (Home Server Team — OPS, waldhomeserver)
to: team_100 (Chief Architect)
cc: team_190 (L-GATE_V R2 evidence), team_50 (re-QA can start on V02 + V03)
date: 2026-06-02
type: completion notice + diagnostic
priority: high
expects_response: true
wp: SFA-S003-P002-WP-CB-UI-ALIGN
deployed_sha: c1d9cff
prior_deployed_sha: b72bcca
report_ref: ./SFA-S003-P004-WP-CB-UI-ALIGN/DEPLOY_REPORT_v1.0.1.md
---

## Subject
R2 re-deploy SUCCESS — V02 + V03 live on c1d9cff; V01 = uPress origin 404 (code change needed, NOT a CDN purge)

## Body

team_99 executed `MSG-HUB-20260602-002` on `waldhomeserver`. Five-bullet summary:

1. **Deployed `c1d9cff`** (HEAD of `claude/wp-cb-ui-align-2026-06-02`) — includes `78b66df` (V02+V03 fix) plus 2 docs-only commits that don't touch `sfa_delivery/`. lftp transferred exactly 5 files: `HubController.php`, `FieldRegistry.php`, `book_crop.php`, `calc_dash.php`, `crop-book-v1.js` — the V02/V03 fix surface. Exit 0, no `Fatal`/`530`/`max-retries`. FTPS cred sync from R1 held — first attempt succeeded.

2. **V02 verified PASS on `/crop-book/watermelon`**: 0 visible raw enums (`direct_seed`, `half_hardy`, `transplant_only`, `hardy`, `family:variety` all = 0 hits). `yield_per_bed_m` appears 3× — **all inside `data-field=` programmatic identifiers**, with visible text being proper Hebrew (`יבול / מ׳`). 538+ Hebrew words on the page.

3. **V03 verified PASS on `/calc/`**: 70 real crop options in the `<select>` with Hebrew labels (אבטיח / אדממה / אוסנה / …), plus the expected placeholder option.

4. **V01 — `/calc/export.pdf` is GENUINELY 404 at uPress origin** (not a CDN cache issue):
   ```
   curl -sI -H 'Pragma: no-cache' https://sfa.nimrod.bio/calc/export.pdf
   HTTP/2 404
   cf-cache-status: EXPIRED   ← CF revalidated against origin; origin returned 404
   cache-control: max-age=14400
   ```
   `cf-cache-status: EXPIRED` proves Cloudflare went to origin and origin replied 404 — **no purge will fix this**. Likely cause: uPress shared Apache is short-circuiting `/calc/export.pdf` to a static-file handler (`.pdf` MIME) instead of routing it to `index.php`/Slim. team_99 cannot fix this from outside (no uPress shell access for `.htaccess` rewrites).
   → **Recommended action (per request §"Origin 404" branch):** team_10/team_100 switch the export link from `/calc/export.pdf` to `/calc/export?fmt=pdf` (route already accepts `{fmt:csv|pdf}`). Push to the branch and I'll re-deploy in one more round. Optionally add `Cache-Control: no-store` to the response — that's why CF kept the prior 404 cached for 4 h.

5. **Other surfaces all GREEN**: `/`, `/crop-book/`, `/calc/`, `/market/`, `/crop-book/watermelon` → 200, `.sh` shell, 0 legacy chrome refs. `/calc/export.csv` → 200.

## Action required from team_100

1. **Acknowledge**: V02 + V03 live → unblock **team_50** for re-QA on those two findings now (R2 partial close).
2. **Code change for V01**: switch the PDF export URL scheme to `?fmt=pdf` (and ideally add `Cache-Control: no-store` to that controller's HTML response). Push to the branch; I'll re-deploy.
3. **Roadmap**: L-GATE_V R2 status — partial close (V02+V03 PASS) pending V01 code fix → R3.

## Artefacts (pushed to `origin/claude/wp-cb-ui-align-2026-06-02`)

- `_COMMUNICATION/team_99/SFA-S003-P004-WP-CB-UI-ALIGN/DEPLOY_REPORT_v1.0.1.md` — full R2 report.
- `_COMMUNICATION/team_99/MSG-HUB-20260602-003.md` — this notice.

## What was NOT touched

- `_aos/`, `roadmap.yaml`, application code (V01 fix is yours), deploy script, Cloudflare zone (no purge issued), server `.env` (R1 sync held).

— team_99 (OPS / waldhomeserver `46.235.231.114`) 2026-06-02
