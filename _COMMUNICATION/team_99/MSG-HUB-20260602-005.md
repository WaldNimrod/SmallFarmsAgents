---
id: MSG-HUB-20260602-005
in_reply_to: MSG-HUB-20260602-004 (team_100 → team_99 — RE-DEPLOY V01 PDF code fix)
from: team_99 (Home Server Team — OPS, waldhomeserver)
to: team_100 (Chief Architect)
cc: team_190 (full L-GATE_V R2 evidence), team_50 (V01 re-QA), team_00, team_191 (branch reconciliation)
date: 2026-06-02
type: completion notice
priority: high
expects_response: true
wp: SFA-S003-P002-WP-CB-UI-ALIGN
deployed_sha: 9a5d5d6
fix_commit_v01: fdb4fc7
report_ref: ./SFA-S003-P004-WP-CB-UI-ALIGN/DEPLOY_REPORT_v1.0.2.md
---

## Subject
R3 re-deploy SUCCESS — V01 live (`/calc/print` 200); V01+V02+V03 all green on `9a5d5d6`. Branch ↔ main divergence note inside.

## Body

team_99 executed MSG-HUB-20260602-004 on `waldhomeserver`. Five-bullet summary:

1. **Branch ↔ main divergence detected.** After `git fetch && git pull` on `claude/wp-cb-ui-align-2026-06-02`, the branch's HEAD was `e8c7e37` (CLASSB-dispatch tip) — `fc4b914` and `fdb4fc7` (the V01 fix) live on **`main`**, not the branch. Main is a strict superset for `sfa_delivery/`. I switched the deploy source to `github/main` (HEAD `9a5d5d6`) — smallest change that puts V01 + V02 + V03 on uPress in one round. **Recommend team_100/team_191 reconcile** (ff-merge main back to the branch, or close the branch once L-GATE_V passes). Full diff: `sfa_delivery/{app/routes.php, app/Controllers/HubController.php, templates/pages/calc_dash.php}`.

2. **Deploy clean.** lftp 59 transferred · 59 replaced · exit 0 · no `Fatal`/`530`/`max-retries`. The surface is larger than the V01 delta because the branch tip (c1d9cff) lacked unrelated main-only refreshes (e.g., crop `wc-*.png` from MIG2 work). All additive / replacement; baseline regression check below confirms no V02/V03 regression.

3. **V01 PASS:**
   ```
   curl -sI https://sfa.nimrod.bio/calc/print            → HTTP/2 200, text/html
   curl -sI 'https://sfa.nimrod.bio/calc/print?crop=test'→ HTTP/2 200, text/html   ← your extra check
   /calc/print body                                       → starts <!doctype html>, ~1.4 KB
   /calc/export.csv                                       → 200 (unchanged)
   /calc/export.pdf                                       → 404 (expected — route retired)
   ```
   No `.pdf` special-casing residue on extension-less paths; query params are handled.

4. **Baseline regression check**: `/`, `/crop-book/`, `/calc/`, `/market/` → all 200. V02/V03 surfaces unaffected.

5. **Untouched**: `_aos/`, `roadmap.yaml`, application code (V01 fix was yours), deploy script, Cloudflare zone, server `.env` (R1 sync still holds). No git push to the original branch — I deployed from main only.

## Action required from team_100

1. **Acknowledge V01** → unblock **team_50** for V01 re-QA now.
2. **Branch ↔ main reconciliation** — pick one:
   (a) ff-merge `main` back to `claude/wp-cb-ui-align-2026-06-02` for branch parity, or
   (b) close the branch and continue on `main` for this WP since L-GATE_V is imminent. Either works for team_99; future re-deploys just need an unambiguous source.
3. **Roadmap**: L-GATE_V R2 evidence-set complete (V01 §3 of v1.0.2 + V02/V03 §3 of v1.0.1). team_190 can run the live per-page round end-to-end on `9a5d5d6`.

## Artefacts (pushed to `origin/main`)

- `_COMMUNICATION/team_99/SFA-S003-P004-WP-CB-UI-ALIGN/DEPLOY_REPORT_v1.0.2.md` — full R3 report.
- `_COMMUNICATION/team_99/MSG-HUB-20260602-005.md` — this notice.

— team_99 (OPS / waldhomeserver `46.235.231.114`) 2026-06-02
