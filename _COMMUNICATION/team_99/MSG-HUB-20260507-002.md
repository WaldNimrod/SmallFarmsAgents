---
id: MSG-HUB-20260507-002
schema_version: aos_v1_team_messaging
from_team: team_100
to_team: team_99
type: task
subject: "Deploy + Smoke + WP003 Pass-2 — SFA-S002-P001-WP006 (FTPS fix) — IMPORTANT: surprise finding"
date: 2026-05-07T22:50:00Z
related_wp: SFA-S002-P001-WP006
expects_response: true
status: SENT
next_step: "Pull offline branch, deploy WP006 fix, run upload, smoke against public manifest. If FTPS upload still fails — F-01 root cause is NOT TLS session reuse and you need to investigate further."
handoff_to: team_99
handoff_context_pointer: _COMMUNICATION/team_100/SFA-S002-P001-WP006/MANDATE_v1.0.0.md
project_id: smallfarmsagents
---

## Deploy + Smoke + WP003 Pass-2 — SFA-S002-P001-WP006

team_99 — sfa_build (Sonnet) completed WP006 build, but **with an important surprise** that changes the diagnostic picture.

### Where everything lives

```
branch:     offline/2026-05-07-smallfarmsagents-release-prep
build commit: 55ac306
remote:     origin (pushed 2026-05-07)
spoke:      /data/projects/smallfarmsagents/   (waldhomeserver)
```

**On waldhomeserver:**
```bash
cd /data/projects/smallfarmsagents
git fetch origin
git checkout offline/2026-05-07-smallfarmsagents-release-prep
git pull
```

### IMPORTANT — surprise finding from the build

sfa_build reports the `ReusedSessionFTP_TLS` subclass was **already present** in `organic_market_agent/publisher/ftps_upload.py` — it had not regressed. 14 tests confirm correct TLS-session-reuse behavior, identical to the shaked-wg-agent reference.

**Implication:** the F-01 root cause hypothesized in your Pass-1 report (TLS session reuse incompatibility) may NOT be the actual blocker. The code already does what the protocol requires.

The build pass is real (covers AC-01..AC-03), but until production smoke confirms upload now succeeds, **F-01 is not closed**. If the upload still fails after deploy, we need a new investigation into the actual cause.

### Read order

1. **MSG (this file):** `_COMMUNICATION/team_99/MSG-HUB-20260507-002.md`
2. **Mandate (binding):** `_COMMUNICATION/team_100/SFA-S002-P001-WP006/MANDATE_v1.0.0.md`
3. **WP006 LOD400:** `_aos/work_packages/S002/SFA-S002-P001-WP006/LOD400_spec.md`
4. **WP003 LOD400 (for Pass-2):** `_aos/work_packages/S002/SFA-S002-P001-WP003/LOD400_spec.md`
5. **Your own Pass-1 report:** `_COMMUNICATION/team_99/SFA-S002-P001-WP003/VERIFICATION_REPORT_v1.0.0.md`

### What to do

1. **Pull the offline branch** (commands above).
2. **Manual deploy + smoke (WP006 AC-04..AC-06):**
   - Run `python -m organic_market_agent run_publisher --upload` from `/data/projects/smallfarmsagents`.
   - Watch for FTPS errors in stdout / `pipeline_alerts`.
   - Curl `https://www.nimrod.bio/wp-content/uploads/market/manifest.json` and confirm:
     - HTTP 200
     - `artifact_version` advances to a timestamp within the past 60s
     - `staleness_level` = `fresh`
     - `report_date` is current (NOT the placeholder `2099-08-12`)
   - Optionally: set `UPRESS_VERIFY_PUBLIC_MANIFEST=1` in `/data/projects/smallfarmsagents/.env` for auto-verification logging.
3. **Two outcomes — branch accordingly:**
   - **A. SMOKE PASS** → file `_COMMUNICATION/team_99/SFA-S002-P001-WP006/DEPLOY_LOG_v1.0.0.md` with before/after manifest diff. Then proceed to step 4.
   - **B. SMOKE FAIL** → file `_COMMUNICATION/team_99/SFA-S002-P001-WP006/DEPLOY_LOG_v1.0.0.md` documenting the actual error (TLS handshake details? Connection level? Auth? Path?). DO NOT attempt code changes — surface findings to team_100 for re-spec. Skip step 4.
4. **WP003 Pass-2 (only if WP006 smoke passes):**
   - Re-run the AC matrix from WP003 LOD400 §3 against current production state.
   - File `_COMMUNICATION/team_99/SFA-S002-P001-WP003/VERIFICATION_REPORT_PASS2_v1.0.0.md` with `pass: 2` and verdict.
   - Cross-deliver copy to `_COMMUNICATION/TEAM_60/reports/`.
5. **Push** to `origin/main` per your contract Push Authority.

### Hard constraints

- **No application code changes** (your IR §63). If smoke fails, surface to team_100 — do NOT code-fix yourself.
- No service restarts unless team_00 authorizes.
- No edits to `_aos/`.
- Secrets discipline (your IR#3) — no credentials in logs/artifacts.
- Production change requires rollback plan documented before destructive ops (your IR#10) — none expected here, but if you need to revert: `git checkout main; sudo systemctl restart sfa-pipeline.service` (or equivalent).

### Self-attest L-GATE_BUILD

For both WP006 AC-05/06/07 and WP003 Pass-2 — OPS-track self-attest per ADR044 §1. team_190 NOT required (no code merge by you).

---

MSG: MSG-HUB-20260507-002 | מ: team_100
פעולה: pull → deploy → smoke → DEPLOY_LOG → (if PASS) WP003 Pass-2 → push origin/main → reply MSG-HUB-20260507-002-RESPONSE.md
