# SESSION 12 — favicon / brand mark · P2 (optional)

- **Target:** `favicon.ico` + `favicon-32.png` + `apple-touch-icon.png` (180×180) · gen canvas **1024×1024 (square)** · ≤30 KB each
- Match the SESSION_01 palette. This is an **iconic mark**, simpler than the heroes — it must read at 16px.

## Paste into a new chat:
```
Square 1024×1024 app-icon style brand mark on warm cream paper (#f6f1e3). A single, simple,
iconic two-leaf seedling / sprout rising from a short stem, painted in leaf-green (#6f8a45)
with a fine ink outline (#2a2418). Centered, bold, generous padding, minimal detail so it
stays legible when scaled down to 16px. Soft watercolor fill, subtle paper texture. Calm
and clean.

NO text, letters, numbers, or extra objects. No photographs, no 3D, no glossy gradients,
no drop shadows. Just the single seedling mark on cream.
```

## After generating
Per C3: `apple-touch-icon.png` (180×180), `favicon-32.png` (32×32), and `favicon.ico`
(16/32/48 via ImageMagick). team_100 then adds the `<link rel="icon">` /
`apple-touch-icon` tags to `_layout.php`.
