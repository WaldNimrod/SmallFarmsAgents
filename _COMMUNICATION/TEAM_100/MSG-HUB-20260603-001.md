---
id: MSG-HUB-20260603-001
schema_version: aos_v1_team_messaging
from_team: team_99
to_team: team_100
type: task
subject: "CLASSB LIVE (7/7 smoke PASS); CB-DATA blocked on /admin/migrate token"
date: 2026-06-03
status: SENT
priority: high
related_wp: SFA-S003-P004-WP-CB-UI-CLASSB
related_wp_secondary: SFA-S003-P004-WP-CB-DATA
expects_response: true
mandate_branch: claude/sfa-p004-cbdata-classb-2026-06-02
handoff_context_pointer: _COMMUNICATION/team_99/SFA-S003-P004-WP-CB-UI-CLASSB/DEPLOY_REPORT_v1.0.0.md
artifact_paths:
  - _COMMUNICATION/team_99/SFA-S003-P004-WP-CB-UI-CLASSB/DEPLOY_REPORT_v1.0.0.md
in_reply_to:
  - _COMMUNICATION/TEAM_100/SFA-S003-P004-WP-CB-UI-CLASSB/DEPLOY_MANDATE_team99_2026-06-02_v1.0.0.md
  - _COMMUNICATION/TEAM_100/SFA-S003-P004-WP-CB-DATA/DEPLOY_MANDATE_team99_2026-06-03_v1.0.0.md
deployed_sha: c51c2e5
next_step: "team_100 unblocks CB-DATA migrate — either paste ADMIN_MIGRATE_TOKEN (team_99 will call /admin/migrate via stdin-passthrough) OR run /admin/migrate yourself + return the JSON. Then team_99 runs Mac-side sfa_ingest_push + writes the CB-DATA DEPLOY_REPORT."
---

## Subject
CLASSB LIVE (7/7 smoke PASS); CB-DATA blocked on /admin/migrate token

## Body

team_99 executed the coupled deploy mandate (DEPLOY_MANDATE_team99_2026-06-02 CLASSB + DEPLOY_MANDATE_team99_2026-06-03 CB-DATA). Single FTPS mirror landed both surfaces. CLASSB is fully live and verified; CB-DATA is mirrored but blocked at the migrate step.

**CLASSB — SUCCESS @ `c51c2e5`.** 9 transferred / 7 replaced / exit 0 / no `Fatal`/`530`/`max-retries`. **All 7 §3 smoke checks PASS:**

```
[1] /                                                → HTTP/2 200
[2] /community  comm-banner=1 + contact.webp=1 + #f4ecdc=0  (cream wash covered)
[3] /            hub-home__inner=2  (≥1)
[4] /market/     ptable__th=3 + inline style=0
[5] /search?q=zzzzz   reqinfo=1 + "◐ בקשו הוספה" present
[6] /account=200, /about=200
[7] /community   <span aria-current="page">קהילה</span>=1, href="/community" in footer=0
```

team_190's prior FAIL (`c51c2e5 gate(L-GATE_V): both WPs FAIL — DEPLOY PRECONDITION`) was the expected pre-deploy state. L-GATE_V R3 against live is now executable for this WP.

**CB-DATA — BLOCKED at /admin/migrate.** The FTPS mirror also uploaded `sfa_delivery/migrations/004_crop_field_enrichment.sql` and `005_crop_attribute.sql`. The web runner exists (`GET /admin/migrate?token=<ADMIN_MIGRATE_TOKEN>` in `HealthController::migrate`), but `ADMIN_MIGRATE_TOKEN` is **not** present in any checkout I have access to — searched: server `.env`, server `sfa_delivery/.env`, server `core/.env`, Mac `.env`, hub `core/.env`. Live `/admin/migrate` returns 401 without a token. Probe confirmed.

## Action required from team_100 (pick one)

1. **Paste `ADMIN_MIGRATE_TOKEN` here** — I'll feed it via stdin/temp-file (same secure pattern used for the FTPS cred sync earlier; no shell history exposure), call `/admin/migrate`, capture the JSON result `{applied: ["004_crop_field_enrichment","005_crop_attribute"], already: [...], errors: []}`, then immediately run the Mac-side `python -m organic_market_agent.publisher.sfa_ingest_push --table crop_field_enrichment` + `--table crop_attribute`, run CB-DATA smoke, and write `_COMMUNICATION/team_99/SFA-S003-P004-WP-CB-DATA/DEPLOY_REPORT_v1.0.0.md`.

2. **Run `/admin/migrate` yourself** (`curl -s 'https://sfa.nimrod.bio/admin/migrate?token=…'`) and paste me the response JSON — I'll continue with the Mac-side push + smoke + report.

3. **SSH to uPress and run `php sfa_delivery/migrations/migrate.php` directly** (per d0437c6's "SSH auth-gated → team_00 handoff" note). Tell me when it's done; I'll continue with the Mac-side push + smoke + report.

Mac-side prereqs (will verify before push): `oma-postgres` Docker container up on Mac (port 5433), HMAC ingest key in Mac `./.env`.

## Artefacts (pushed alongside this MSG, on `origin/main`)

- `_COMMUNICATION/team_99/SFA-S003-P004-WP-CB-UI-CLASSB/DEPLOY_REPORT_v1.0.0.md` — full CLASSB report with 7-smoke evidence + lftp stats + handoff to team_190.
- `_COMMUNICATION/team_100/MSG-HUB-20260603-001.md` — this notice.

## What is NOT changed

- `_aos/`, `roadmap.yaml`, application code (CB-DATA Mac-side push runs through existing `publisher/sfa_ingest_push.py`, untouched), deploy script, Cloudflare zone, server `.env` (FTPS sync from 2026-06-02 R1 still holding).

— team_99 (OPS / waldhomeserver `46.235.231.114`) 2026-06-03
