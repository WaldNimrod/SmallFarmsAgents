# REGISTER — SFA-S003-P004-WP-CB-DATA-CURRENCY — production crop-data catch-up (incremental)

**Date:** 2026-06-12 · **By:** team_100 (team_00 directive) · **Status:** DEFERRED · **LOD:** REGISTER · **Effort:** MEDIUM
**Depends on:** `SFA-S003-P004-WP-CB-DATA-API` (must flow through the validated incremental API — **NOT** `seed --all`).

## Why deferred (team_00, 2026-06-12)
Production is intentionally behind the local dataset (clean 70 crops live vs more locally) — the publish whitelist
holds back crops that are below the data-completeness bar. team_00: **do not push thin crops; this is recurring
drift that needs data completion first**, and it must be done incrementally once the Data-API foundation exists.

## Scope (the gathered data tails — build when DATA-API lands)
1. **Catch-up deploy (70 → full set)** — push the accumulated, never-deployed enriched crops, **incrementally + scoped**
   (`sfa_ingest_push --crop-ids`), only those meeting the completeness bar. Flag + measure the drift each time.
2. **3 thin keep-crops** — celeriac / chinese-cabbage / hot-pepper: enrich to the bar, then scoped-push (held local-only today).
3. **Beans 2-crop model** (team_00 canon 2026-06-11): exactly two bean crops — `שעועית מטפסת` (pole, **default**) and
   `שעועית שיח` (bush); varieties nest by type, **bush only when explicit, else climbing**. Fix the importer
   bush/pole key-mapping that skips 3 crops. (Recorded in the crop-taxonomy canon.)
4. **3 hydro-only geresh-filename crops** (פאק צ'וי / קולרובי / רוקט) — filename mismatch leaves them unmapped at load; fix the mapping.
5. **4 JMF FT guides** (phytoprotection / biopesticide / flameweed / nurseryseeding) — integrate.
6. **`seed --all` AC-05 hard-fail guard** — fold into / coordinate with DATA-API (write-time AC-05 enforcement).

## Already resolved (record only — no action)
- **Duplicate crop #95 (`salad-mix`)** — RESOLVED by the WP-CB-SRC-SWEEP taxonomy fix (`constants.py`: `Salad Mix → עלי בייבי`;
  `test_seed_taxonomy_fix.py`; validated "73 crops / 0 duplicate-name crops"). Closed.
- **`idan_planner` forbidden `yield_per_m2_kg` emit** — REMOVED at source (this session's tails cleanup).

## Notes
- Backend/pipeline + scoped delivery-tier ingest; data-heavy; real prod-deploy risk → its own validation + deploy gate.
- Split out from the delivery-tier UI tails per team_00 (separate WP).
