---
id: MSG-HUB-20260603-002
schema_version: aos_v1_team_messaging
from_team: team_99
to_team: team_100
type: status
subject: "UPDATE — live probe confirms CB-DATA migrate NOT run (1146 Table not found); still standing by for token/run"
date: 2026-06-03
status: SENT
priority: medium
related_wp: SFA-S003-P004-WP-CB-DATA
expects_response: false
in_reply_to:
  - _COMMUNICATION/TEAM_100/MSG-HUB-20260603-001.md
  - _COMMUNICATION/TEAM_100/SFA-S003-P004-WP-CB-DATA/DEPLOY_MANDATE_team99_2026-06-03_v1.0.0.md
related_msg: _COMMUNICATION/TEAM_100/MSG-HUB-20260603-001.md
---

## Subject
UPDATE — live probe confirms CB-DATA migrate NOT run (1146 Table not found); still standing by for token/run

## Body

Followup to MSG-HUB-20260603-001. This is a status update — no new ask; the action ask from MSG-001 still stands.

### What I did since MSG-001 (all non-destructive)

1. **Mac pipeline validation — `--dry-run` for both tables:**
   ```
   crop_field_enrichment   dry-run OK   16 batches   ~767 rows   payloads built + HMAC signed
   crop_attribute          dry-run OK    5 batches   ~243 rows   payloads built + HMAC signed
   ```
   `oma-postgres` Docker container up on Mac (port 5433, healthy); `SFA_INGEST_URL` + `SFA_INGEST_HMAC_SECRET` present in Mac `.env`; publisher CLI accepts both `--table crop_field_enrichment` and `--table crop_attribute`. Mac→origin path is verified working.

2. **1-row real probe (`--limit 1`) to confirm migrate state on uPress:**
   ```
   POST /api/v1/ingest  →  HTTP/1.1 200  (route + HMAC verified)
   {
     "accepted": 0,
     "rejected": 1,
     "errors": [{
       "index": 0,
       "error": "SQLSTATE[42S02]: Base table or view not found:
                 1146 Table 'sfanms2u_SFAUserUiDB.crop_field_enrichment' doesn't exist"
     }]
   }
   ```
   The IngestController route (deployed in this batch's mirror) works correctly — HMAC accepted, payload parsed, DB write attempted, then failed because the destination table doesn't exist. **Definitive evidence that migrations 004 + 005 have not been applied yet.** No row was inserted (uPress MySQL rejected at INSERT time).

### Where we stand

| | |
|---|---|
| CLASSB | LIVE + 7/7 smoke + report + MSG-001 (no change from earlier) |
| Mac → uPress ingest pipeline | end-to-end VALIDATED (dry-run + 1-row probe both clean) |
| uPress MySQL state | table `crop_field_enrichment` (and almost certainly `crop_attribute`) absent → migrate 004 + 005 NOT applied |
| Live `SFA_CROP_BOOK` on `/calc/` | the file contains the JS *alias-map* comment but no populated data object — consistent with the table-absent state |

### Unblock — no change from MSG-001

Same 3 options stand: paste `ADMIN_MIGRATE_TOKEN`, run `/admin/migrate?token=…` yourself and paste the JSON, or SSH `php sfa_delivery/migrations/migrate.php`. Once any of those lands, ~3 commands away from CB-DATA closure.

## What is NOT changed

`_aos/`, `roadmap.yaml`, application code, deploy script, server `.env`, uPress state (the 1-row probe inserted nothing because of the table-not-found rejection).

— team_99 (OPS / waldhomeserver — Mac-side ingest verified, uPress migrate still pending) 2026-06-03
