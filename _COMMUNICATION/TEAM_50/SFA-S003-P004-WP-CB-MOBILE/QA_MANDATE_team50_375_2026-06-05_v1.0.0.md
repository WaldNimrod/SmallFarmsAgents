# VISUAL QA MANDATE — WP-CB-MOBILE @375 — team_100 → team_50 — v1.0.0

**Date:** 2026-06-05 · **From:** team_100 · **To:** team_50 (visual QA — external/non-Claude engine per IR#1/#5)
**WP:** SFA-S003-P004-WP-CB-MOBILE · **Run AFTER** team_99 deploys to https://sfa.nimrod.bio (confirm the live `?v=` bumped first).
**Method:** CDP / browser-QA at **375px** (RTL). Harness: `_aos/lean-kit/modules/validation-quality/scripts/qa/qa_probe.mjs`. **Do not** validate layout with curl alone (see BROWSER_QA_HARNESS_CANON).

## Why this WP exists
team_00 launch blocker: crop-book + market + calculator were not launch-ready on **mobile (375)**. This WP is the full team_35 v4 remediation. Desktop was already launch-quality — **flag any desktop regression** (the ONLY sanctioned desktop changes are D1 market table-default + D2 type floor).

## QA checklist (from MOBILE_DESIGN_v4.0.0.md §8) — verify each @375
1. **No clipping / horizontal overflow**; tight margins throughout (global density pass).
2. **Hub (`/`):** real `module-*.png` launchers (NOT hero washes); coming tiles icon-only; CTA block present (data / form / WhatsApp).
3. **Crop cards (book list):** small thumbnail (NO background wash); **crop name dominates, DTM is just a chip**; in-season `🌱/🪴` badge on this-month crops; CTA foot.
4. **Crop page (defects #1,#2):**
   - **Hero does NOT overlap** (logo blob + title + watercolor no longer collide) — this was the #1 defect.
   - **Planting calendar legible**; **NO raw token** (`IL_general`, `seed`, `spring`, …) anywhere — all Hebrew (region→כל הארץ/צפון/…, activity→זריעה/שתילה, season→אביב/…).
   - **Depths:** Simple = genuinely minimal (essentials + calendar + one key/topic); Full = overview + all 17 fields; Deep = Full + per-datum ranges/varieties/sources. NOTE (expected, not a bug): **Deep source pills are omitted where data has no provenance**; variety ranges show only with ≥2 varieties.
5. **Market (defect #3):** disclaimer collapsed by default → expands on tap; **11 category chips incl. סלים**; **price reads correctly RTL** (number then unit, no digit/unit reorder); **table is the default view** + cards toggle works & persists. Confirm the ~12,500px one-card-per-row scroll is GONE.
6. **Calculator:** 6 goal buttons + dropdown (14 total); session list accumulates across calcs + export-all; assumptions editable from the result. NOTE (expected, not a bug): **8 of the 14 goals show a "בפיתוח" notice on compute** (only 6 have live math) — verify they show the notice, NOT a wrong/blank number.
7. **About:** content-first (intro + 4 points); tiers below as a secondary expansion; CTAs.
8. **CTAs:** suggestion path is an **inline form, NOT WhatsApp**; WhatsApp only on the custom-for-farm card. Type floor honored (also check desktop isn't too small now / not broken).

## Report
GO / GO-WITH-FIXES / NO-GO + per-item evidence (screenshots @375). Return to team_100 → on GO, team_100 records LOD500. team_35 does NOT screenshot — visual QA is yours.
