# QA Report — Gate G2 — M2 Collection Layer

**Date:** 2026-03-30  
**From:** Team 50 (QA)  
**Gate:** G2  
**Decision:** **FAIL** (gate cannot open)  
**In response to:** `_COMMUNICATION/TEAM_50/reports/2026-03-30_G2_REVIEW_REQUEST_M2_TEAM50.md`  
**Evidence package reviewed:** `_COMMUNICATION/TEAM_10/reports/2026-03-30_M2_COMPLETE_TEAM10.md`  
**Mandate:** `_COMMUNICATION/TEAM_50/QA_MANDATE_G2.md`

---

## Summary

Team 10 requested formal G2 sign-off. Per `QA_MANDATE_G2.md`, **Gate G1 must be formally open** before G2 can open. There is **no** Team 50 G1 sign-off report under `_COMMUNICATION/TEAM_50/reports/` (no `*G1*` QA report on file as of this date). **Therefore Gate G2 is blocked regardless of M2 implementation quality.**

Team 50 additionally executed **static checks** (T10–T12-style greps) and **unit tests** for collectors/parsers on the workspace machine. Full mandate execution (T02–T09: live ingestion, SQL evidence, T06/T07 exact queries, regression row counts vs G1 baseline) was **not** reproduced independently in this QA session because no compliant shared database session was available to Team 50 here; findings below separate **prerequisite blockers**, **Team 50–executed** results, and **third-party evidence gaps** relative to the mandate.

---

## Prerequisite — Gate G1

| Criterion | Source | Result |
|-----------|--------|--------|
| Gate G1 formally open (Team 50 sign-off filed) | `_COMMUNICATION/TEAM_50/QA_MANDATE_G2.md` § Prerequisites | **FAIL** — no `*_QA_G1_TEAM50.md` or equivalent G1 sign-off found in `_COMMUNICATION/TEAM_50/reports/` |

**Effect:** Mandate states Team 10 completion report + **G1 open**. G1 is not on file → **G2 cannot open.**

---

## Unit Tests (T01)

**Mandate command:**

```bash
python -m pytest tests/test_collectors.py tests/test_parsers.py -v
```

### Team 50 execution (workspace — 2026-03-30)

**Command run:**

```bash
cd /Users/nimrod/Documents/SmallFarmsAgents && python3 -m pytest tests/test_collectors.py tests/test_parsers.py -v
```

**Environment note:** Interpreter was **Python 3.9.6** (below mandate environment reminder of **3.11+**). This run is **not** sufficient to satisfy T01 for gate purposes; it is supplementary signal only.

**Output (abridged):**

```text
collecting ... collected 20 items
tests/test_collectors.py::test_select_collector_easyfarm PASSED
... (8 more collector tests) ...
tests/test_parsers.py::test_easyfarm_extracts_two_items PASSED
... (9 more parser tests) ...
============================== 20 passed in 0.40s ==============================
```

**Result:** **PASS** (20/20) on this machine — **does not replace** mandated **Python 3.11+** run.

### Team 10 cited execution (third-party)

From `2026-03-30_M2_COMPLETE_TEAM10.md`:

```text
python3.11 -m pytest tests/ -v
============================== 27 passed in 0.26s ==============================
```

Breakdown stated: `test_collectors.py` (10), `test_parsers.py` (10), `test_db_health.py` (7).

**Team 50 assessment:** T01 **would PASS** if re-run under **Python 3.11+** with identical repo revision and pasted verbatim output attached; Team 50 has **not** independently verified the `python3.11` run in this session.

---

## QA Tests — Mandate T02–T09 (live DB / ingestion)

| # | Mandate ID | Description | Team 50 result | Evidence / notes |
|---|------------|-------------|----------------|------------------|
| 1 | T02 | Live ingestion; exit 0; ≥3 sources succeeded | **NOT EXECUTED** (Team 50) | Team 10 report: `partial`, `succeeded=16` first run; **Team 50 did not capture CLI or verify `ingestion_runs` SQL** |
| 2 | T03 | `raw_assets` shape, checksum length 64, filesystem layout | **NOT EXECUTED** | Team 10: 24 `raw_assets`; no mandate SQL/fs listing in report |
| 3 | T04 | `raw_extracted_items` ≥50, ≥70% named, ≥3 fetch runs | **EVIDENCE ONLY (Team 10)** | Team 10: **3210** rows — satisfies **≥50**; named ratio and fetch-run coverage **not pasted as mandate queries** |
| 4 | T05 | `source_fetch_runs` breakdown; ≥3 success; no `running` | **EVIDENCE PARTIAL (Team 10)** | Team 10: success=24, skipped=8, failed=8 (cumulative over **two** runs); **not** split by `ingestion_run_id` as mandate implies |
| 5 | T06 | Second run: `new_assets=0`; all statuses `skipped` for that run | **NOT VERIFIED** | Team 10 states dedup/skipped in logs; second run summary shows **`succeeded=8`** per CLI — **conflicts with strict reading of mandate** (“all `source_fetch_runs.status = 'skipped'` for the second run”) unless clarified. **Mandatory:** paste exact SQL from `QA_MANDATE_G2.md` T06 for `MAX(id)` ingestion run |
| 6 | T07 | Bad URL; `failed`, `retry_count≥1`, `error_message`, `log_entries` ERROR | **NOT EXECUTED** | Not documented in Team 10 completion report |
| 7 | T08 | `normalized_observations` count = 0 | **NOT VERIFIED** | Not stated in Team 10 report |
| 8 | T09 | M1 seed tables unchanged vs **G1-confirmed** counts | **NOT VERIFIED** | No G1 baseline on file; Team 10 report does not include T09 union query |

---

## Code Review — T10, T11, T12 (Team 50 executed)

### T10 — No hardcoded Hebrew product names (mandate sample strings)

**Commands (equivalent to mandate intent):**

```bash
# ripgrep via workspace search: organic_market_agent/collectors + parsers
# Pattern: עגבניה|מלפפון|גזר|בצל|תפוח אדמה
```

**Result:** **PASS** — **0 matches** in `organic_market_agent/collectors/` and `organic_market_agent/parsers/`.

### T10 — Hardcoded `http://` / `https://` in `*.py` (collectors + parsers)

**Result:** **PASS** — **0 matches** for `https?://` in `organic_market_agent/collectors/**/*.py` and `organic_market_agent/parsers/**/*.py`.

### T11 — No live HTTP in unit tests

**Scope:** `tests/test_collectors.py`, `tests/test_parsers.py`  
**Patterns:** `httpx.get`, `httpx.Client`, `requests.get`

**Result:** **PASS** — **0 matches**.

### T12 — No `session.query(` (collectors, parsers, scheduler)

**Result:** **PASS** — **0 matches** in `organic_market_agent/collectors/`, `organic_market_agent/parsers/`, `organic_market_agent/scheduler/`.

---

## Critical Findings (FAIL — block gate)

1. **Prerequisite:** **Gate G1 is not formally open** — missing Team 50 G1 written sign-off in `_COMMUNICATION/TEAM_50/reports/`. **Blocks G2 per `QA_MANDATE_G2.md`.**

2. **Incomplete independent QA evidence for Critical tests T02–T09:** Mandate requires full `pytest` output (T01), full CLI ingestion output (T02), and **all SQL outputs** (T03–T09). Team 10’s report supports some thresholds (e.g. row counts) but **does not substitute** for Team 50–attested query results; **T06/T07/T08/T09** are **not** closed from Team 50’s perspective.

3. **T06 ambiguity vs mandate:** Second-run CLI shows **`succeeded=8`** while mandate requires **all** `source_fetch_runs` for the **second** `ingestion_runs.id` to be **`skipped`** and **`new_assets = 0`**. This must be resolved with **exact T06 SQL** from the mandate; if behavior is intentional (e.g. semantic meaning of “succeeded”), **Team 100** must document acceptance or the mandate must be updated.

---

## Minor Findings (WARNING — do not unblock alone)

1. **Environment drift:** Team 10 verification used **PostgreSQL in Docker** (`postgres:15-alpine` on port 55433) while stack communication locks **direct PostgreSQL** for production. G2 mandate text is a **reminder** only (unlike G1’s explicit Docker fail). Recommend G2 sign-off runs repeat on **direct install** for parity with roadmap.

2. **Team 10 test DB mutation:** Report notes `retry_policy_json` set to `max_retries: 0` on all profiles **in test DB only** — acceptable for engineering evidence but must not be assumed for production DB; QA should use a clean profile state for T07 retry tests.

---

## Required Actions to Open Gate

- [ ] **Team 50:** File and close **G1** QA report with PASS (or approved CONDITIONAL PASS) — `_COMMUNICATION/TEAM_50/reports/{date}_QA_G1_TEAM50.md`.
- [ ] **Team 10 / operator:** Re-run **full** `QA_MANDATE_G2.md` checklist under **Python 3.11+**; attach **verbatim** outputs for T01–T09 (CLI + SQL + greps already satisfied can reference this report).
- [ ] **Team 10:** Provide **T06** and **T07** evidence exactly as specified in the mandate (queries + results); reconcile **second-run “succeeded” vs all-`skipped`** expectation.
- [ ] **Team 100:** If T06 behavior is correct by architecture, document **formal deviation** or amend mandate text so QA can score consistently.

---

## [USER ACTION REQUIRED]

**Nimrod:** Approve ordering: **G1 QA sign-off first**, then Team 50 re-runs or accepts pasted evidence for **full G2 mandate** on a compliant environment. Until then, **do not treat M2 as gated.**

---

## Gate Decision

**Gate G2:** **BLOCKED**

**Rationale:** Prerequisite **G1 not open**; Critical mandate items **T02–T09** not fully evidenced by Team 50 independent verification; **T06** requires clarification against mandate SQL.

---

## Canonical cross-reference

| Document | Role |
|----------|------|
| `2026-03-30_G2_REVIEW_REQUEST_M2_TEAM50.md` | Incoming request (Team 10) |
| `QA_MANDATE_G2.md` | Acceptance tests (Team 100 → Team 50) |
| `2026-03-30_M2_COMPLETE_TEAM10.md` | Implementer evidence (reviewed, not sufficient alone for G2 OPEN) |
