---
document_type: QA_FINDINGS_REPORT
version: "1.0"
---

# QA Findings Report — Gate G5

**Report ID:** QA-RPT-20260331-G5  
**QA Review Request:** (Team 100 G5 handoff / M5 completion — informal)  
**From:** Team 50 (QA)  
**To:** Team 100 (Architecture)  
**CC:** Team 10 (Feature Dev), Team 20 (Infrastructure)  
**Date:** 2026-03-31  
**Gate:** G5 — M5 Admin UI (authenticated writes + audit + triggers)  
**QA Mandate executed:** `_COMMUNICATION/TEAM_50/QA_MANDATE_G5.md`

---

## 1. Environment Verified

| Check | Result |
|-------|--------|
| Python version | **3.9.6** (`.venv`) — project policy prefers 3.11+; **not** a G5 mandate hard fail |
| PostgreSQL | Direct install via `DATABASE_URL` in repo `.env` (credentials not logged) — connected |
| Alembic revision | **015 (head)** — PASS |
| `db.check` | **PASS** (includes `users`, `audit_log`, `observation_flags` index) |
| Port 5001 | Freed stale listener before run; used for `run_admin` during T02–T10 |

**Pre-condition commands (mandate § Pre-conditions):**

```text
alembic current → 015 (head)

pip show flask-login bcrypt:
Name: Flask-Login / Version: 0.6.3
Name: bcrypt / Version: 5.0.0

admin@local (SQLAlchemy): 1 row → ('admin@local', 'admin')
```

---

## 2. Test Results

| Test ID | Test Name | Result | Weight | Notes |
|---------|-----------|--------|--------|-------|
| T01 | `pytest tests/test_admin_routes.py` | PASS | Critical | 11 passed, 0 skipped |
| T02 | Authentication flow | PASS | Critical | T02c used two-step curl (see Evidence) |
| T03 | Read routes (no auth) | PASS | High | 9 paths — all 200 (mandate text says “10”; app exposes 9 listed routes) |
| T04 | Alias creation + DB + audit | PASS | High | `create_alias` / `product_alias` verified |
| T05 | Alias disable | PASS | High | `is_active` false for disabled id |
| T06 | Rule create + disable | PASS | Medium | **`profile_id` required** — not in mandate curl; used `profile_id=1` |
| T07 | Manual run trigger | PASS | Medium | `AFTER = BEFORE + 1` on `ingestion_runs` |
| T08 | Audit log page | PASS | Medium | `grep` count ≥ 3 for mandated substrings |
| T09 | QA flags view | PASS | Medium | HTTP 200 |
| T10 | Regression counts | PASS | High | All thresholds met |
| T11 | Full `pytest tests/` | PASS | Critical | 73 passed, 1 skipped (QA001 — G4 waiver) |

**Score:** 11/11 mandate slices passed (T01–T11).  
**Critical failures:** 0

---

## 3. Evidence

### T01 — Unit tests (`test_admin_routes.py`)

```text
collected 11 items
tests/test_admin_routes.py::test_t01_readonly_get_routes_return_200 PASSED
tests/test_admin_routes.py::test_t02_login_success_sets_session_and_redirects PASSED
tests/test_admin_routes.py::test_t03_login_wrong_password_shows_error PASSED
tests/test_admin_routes.py::test_t04_aliases_new_without_login_redirects_to_login PASSED
tests/test_admin_routes.py::test_t05_aliases_new_with_login_creates_row_and_audit PASSED
tests/test_admin_routes.py::test_t06_disable_alias_with_login PASSED
tests/test_admin_routes.py::test_t07_rules_new_with_login PASSED
tests/test_admin_routes.py::test_t08_rules_disable_with_login PASSED
tests/test_admin_routes.py::test_t09_runs_trigger_creates_ingestion_run PASSED
tests/test_admin_routes.py::test_t10_product_disable_alias_with_login PASSED
tests/test_admin_routes.py::test_t11_audit_lists_prior_actions PASSED
============================== 11 passed in 2.25s ==============================
```

### T02 — Authentication

**T02a** — Unauthenticated `POST /aliases/new`:

```text
T02a POST unauth aliases/new: 302
```

**T02b** — Wrong password (snippet):

```html
<div class="alert alert-danger small" role="alert">אימייל או סיסמה שגויים.</div>
```

**T02c** — Correct password: `curl -L -X POST ...` to `/` after login returned **405** (Werkzeug: POST to `/` not allowed). **Resolution:** `POST /auth/login` (save cookies) then **`GET /`** with same jar → **200**; `grep -c "דשבורד"` → **3**.

### T03 — Read routes

```text
/ -> 200
/sources -> 200
/products -> 200
/unresolved -> 200
/aliases -> 200
/rules -> 200
/runs -> 200
/qa_flags -> 200
/audit -> 200
```

### T04 — Alias creation

- **Alias text:** `test-alias-1774911599` (stored in `/tmp/g5_raw_alias.txt` during run)  
- **POST:** `302` to redirect (no `-L` on POST to avoid 405 on follow-up).  
- **DB:** Rows present for alias_text with `is_active=true` prior to T05 (duplicate rows observed for same text — see Findings).  
- **audit_log:** Latest actions included `('create_alias', 'product_alias', 'Administrator')`.

### T05 — Alias disable

- **ALIAS_ID:** 116 (max id for T04 alias_text)  
- **POST** `/aliases/116/disable`: **302**  
- **DB:** `SELECT is_active FROM product_aliases WHERE id=116` → **false**

### T06 — Rule create + disable

- **`profile_id`:** `1` from `SELECT id FROM normalizer_profiles WHERE is_active = true ORDER BY id LIMIT 1`  
- **POST** `/rules/new` with `rule_kind=exclusion`, `match_pattern=test-exclusion`, … → **302**  
- **DB:** `(id, rule_kind, is_active) = (11, 'ignore_pattern', True)` — UI maps `exclusion` → `ignore_pattern`  
- **POST** `/rules/11/disable` → **302**; `is_active` → **false**

### T07 — Run trigger

```text
BEFORE=54
T07 POST: 302
AFTER=55
```

### T08 — Audit page substrings

```text
grep -c "create_alias\|disable_alias\|trigger_run" → 24
```

### T09 — QA flags

```text
T09 qa_flags: 200
```

### T10 — Regression SQL

```text
sources: 20
products: 29
active_aliases: 103
observations: 317
daily_aggs: 25
```

### T11 — Full pytest (last lines)

```text
..................s..................................................... [ 97%]
..                                                                       [100%]
73 passed, 1 skipped in 2.55s
```

---

## 4. Findings Summary

### Passed tests

- **T01–T11:** All mandate criteria met on this host after pre-flight (Alembic 015, seeded admin, port cleared).

### Notes (non-blocking)

1. **T02c / curl `-L`:** Single command `curl -L -X POST ... login` can follow with a **POST** to `/` → **405**. Use **login POST then GET `/`** with cookie jar. Recommend Team 100 add this footnote to `QA_MANDATE_G5.md`.  
2. **T06 mandate curl:** Omits **`profile_id`**; rule create **fails** without it. Team 50 used `profile_id=1`. Recommend mandate amendment.  
3. **T03 route count:** Mandate table says “10 GET routes”; the listed paths are **nine** — all returned 200.  
4. **T04 DB:** Two `product_aliases` rows with identical `alias_text` observed (likely duplicate POST attempts during 405 debugging). T05 disabled **max(id)**. Team 10 may review uniqueness / UX for double-submit.  
5. **Python 3.9.6:** Below repo policy 3.11+; upgrade path remains Team 20 / environment.

### Skipped tests (T11)

| Suite | Reason |
|-------|--------|
| `test_qa001_outlier_high_price` | Skipped when &lt;11 active sources — **G4 QA001 waiver** applies |

---

## 5. Gate Decision

### GATE G5 — PASS

All critical tests passed. **Gate is open** from Team 50’s perspective.

**Next:** Team 100 architectural sign-off per `QA_MANDATE_G5.md` (“Additional requirement: Team 100 architectural review sign-off”).

---

## 6. Required Actions

| Team | Action | Priority |
|------|--------|----------|
| Team 100 | Formal G5 sign-off; optionally patch `QA_MANDATE_G5.md` (T06 `profile_id`, T02c curl note, T03 route count) | HIGH |
| Team 10 | Optional: investigate duplicate alias rows on double POST | LOW |
| Team 50 | Re-run mandate if mandate text changes materially | AS NEEDED |

---

*Filed by: Team 50 (QA)*  
*Date: 2026-03-31*  
*Gate decision requires Team 100 acknowledgment before implementation proceeds.*
