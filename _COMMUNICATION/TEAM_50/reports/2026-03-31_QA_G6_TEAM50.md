---
document_type: QA_FINDINGS_REPORT
version: "1.0"
---

# QA Findings Report — Gate G6

**Report ID:** QA-RPT-20260331-G6  
**From:** Team 50 (QA)  
**To:** Team 100 (Architecture)  
**CC:** Team 10 (Feature Dev), Team 20 (Infrastructure)  
**Date:** 2026-03-31  
**Gate:** G6 — M6 Automation + Resilience  
**QA Mandate executed:** `_COMMUNICATION/TEAM_50/QA_MANDATE_G6.md`

---

## 1. Environment Verified

| Check | Result |
|-------|--------|
| Python | **3.9.6** (`.venv`) |
| PostgreSQL | Connected via `DATABASE_URL` from repo `.env` (value not logged) |
| Alembic | **016 (head)** — PASS |
| `db.check` | **PASS** (`scheduler_config`, `pipeline_alerts` present) |
| Admin server | `run_admin --port 5001` for manual tests; port freed after QA |

**Pre-condition notes (mandate vs observed):**

| Mandate check | Observed |
|---------------|----------|
| `GET /` unauthenticated → **302** | **200** — [dashboard `index()`](organic_market_agent/admin/routes/dashboard.py) is not `@login_required` (same as G5 read-only dashboard). |
| `scheduler_config` defaults `is_enabled=t` | At start of session one read showed `(is_enabled, hour, minute, retries) = (False, 6, 0, 2)`; toggles during G5/G6 may differ from fresh seed. **Functional** checks (T02c) still validated toggle behavior. |
| Login `POST` then `GET /` with cookies | **302** then **200** — PASS (two-step curl; no `curl -L` on login POST). |

---

## 2. Test Results

| Test ID | Result | Weight | Notes |
|---------|--------|--------|-------|
| T01 | **PASS** | Critical | `pytest tests/ -q` — 85 passed, 1 skipped |
| T02 | **PASS** | High | Scheduler 200; 4 field name hits; toggle flipped `is_enabled` and restored |
| T03 | **PASS** | High | Used `cleanup_enabled=on` (not `cleanup_enabled=true`); DB `7|30|3|60` then restored defaults |
| T04 | **PASS** | High | `AFTER=BEFORE+1`; `source_fetch_runs` for run **461** only **SRC002** |
| T05 | **PASS** | High | Chart grep **5**; `canvas` count **2** |
| T06 | **PARTIAL** | Medium | `pipeline_alerts` **COUNT=6** (≥1). Mandate HTML `grep` for `pipeline_alerts\|alert-danger\|…\|סמן כנקרא` → **0** (dashboard alert panel only renders **unread** rows; nav badge uses `g.unread_alert_count`). |
| T07 | **PASS** | Medium | 302; `cleanup_last_run` non-null; latest audit **`run_cleanup`** |
| T08 | **PASS** | Medium | Runs page **200**; grep `duration|סטטוס|הרץ|מקורות` → **3** |
| T09 | **PASS** | High | `test_runner.py` — **7 passed**, 0 skipped |
| T10 | **PASS** | Critical | All baselines met |

**Critical failures:** 0  
**High failures:** 0  

**Gate rule (mandate):** FAIL if any Critical fails **or** ≥3 High fails → **not triggered**.

---

## 3. Evidence (verbatim / summary)

### Preconditions

```text
alembic current → 016 (head)
to_regclass('public.pipeline_alerts') → pipeline_alerts
GET / unauth → 200
POST /auth/login → 302
GET / with cookies → 200
```

### T01

```text
85 passed, 1 skipped in 3.62s
```

(Skip: `test_qa001_outlier_high_price` — G4 QA001 waiver.)

### T02

```text
T02a scheduler: 200
T02b field grep: 4
T02c toggle1: 302 → AFTER_TOGGLE False (toggled from True); restore: 302
```

### T03

```text
T03 update test: 302
T03 DB after update: (7, 30, 3, 60, True)
T03 restore: 302
```

POST body used: `run_hour=7&run_minute=30&retry_attempts=3&cleanup_enabled=on&cleanup_after_days=60`  
Restore: `run_hour=6&run_minute=0&retry_attempts=2&cleanup_enabled=on&cleanup_after_days=90`

### T04

```text
BEFORE=82 SOURCE=SRC002
T04 POST: 302
AFTER=83
LAST_RUN=461
T04 sfr sources for run 461: ['SRC002']
```

### T05

```text
chartResolution|chartSources|chart.js|Chart( → 5
canvas → 2
```

### T06

```text
pipeline_alerts before T06: 6
T06 trigger full: 302
pipeline_alerts after T06: 6
grep mandate pattern on dashboard HTML → 0
```

### T07

```text
T07 cleanup: 302
cleanup_last_run: 2026-03-30 23:36:10.423809+00:00
latest audit action: run_cleanup
```

### T08

```text
T08 runs: 200
grep duration|סטטוס|הרץ|מקורות → 3
```

### T09

```text
7 passed in 0.32s
```

### T10

```text
sources: 20, products: 29, active_aliases: 110, observations: 359, daily_aggs: 25
```

---

## 4. Findings Summary

### Passed

- Full pytest suite, runner unit tests, scheduler CRUD/toggle, focused ingestion trigger scoping, dashboard charts, cleanup route + audit, runs list UI, regression counts.

### Partial / documentation

- **T06 UI vs mandate:** Table has alert rows, but template does not render the mandate’s exact substrings when there are **no unread** alerts. Recommend Team 100 align T06 grep with Bootstrap 5 markup (`bg-danger` badge in nav) or require an unread row for the test.
- **Pre-condition GET `/`:** Mandate expects 302; product allows anonymous dashboard — mandate or product should converge.

---

## 5. Gate Decision

### GATE G6 — PASS

All **Critical** tests passed; fewer than **3 High** failures. **Medium** T06 HTML literal grep is noted but does not meet the mandate’s aggregate fail rule.

**Next:** Team 100 architectural sign-off per `QA_MANDATE_G6.md`.

---

## 6. Required Actions

| Team | Action |
|------|--------|
| Team 100 | Formal G6 sign-off; optional mandate tweaks (GET `/` code, T06 HTML assertions, T03 document `cleanup_enabled=on`). |
| Team 50 | Re-run mandate after material mandate changes. |

---

*Filed by: Team 50 (QA)*  
*Date: 2026-03-31*
