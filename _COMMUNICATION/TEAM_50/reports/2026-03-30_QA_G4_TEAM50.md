# QA Report — Gate G4 — Aggregation + Local Viewer + Admin Dashboard

**Date:** 2026-03-30  
**From:** Team 50 (QA)  
**Mandate ID:** `QA-MANDATE-20260330-G4`  
**Source:** `_COMMUNICATION/TEAM_50/QA_MANDATE_G4.md`  
**Decision:** **FAIL**  
**Gate G4:** **BLOCKED**

---

## Summary

Prerequisites **014 (head)** and **pytest** baseline are largely met; **Team 50** produced **`output/public/`** artifacts by invoking **`AggregatorEngine`** and **`PublishEngine`** in Python (mandated CLI commands **`run_aggregator`** / **`run_publisher`** are **not** registered on `python -m organic_market_agent`). **T03**, **T04**, **T06** (via manifest + unit coverage), **T07** (viewer with required `--dir`), and **T08** (HTTP 200 on five routes) **pass** on this host. **T05** fails the **second** consistency query (**one** `daily_aggregate` row: `sample_size` ≠ joined observation count). **T01** / full suite include **one skipped** test. **T09** cannot be scored **PASS** without a **recorded pre-M4 baseline** for `raw_extracted_items` / `product_aliases` on a quiet DB. Overall gate **BLOCKED** per mandate §4.

---

## Prerequisites (mandate §1)

| # | Check | Result | Evidence |
|---|--------|--------|----------|
| 1 | `alembic current` → `014 (head)` | **PASS** | `014 (head)` |
| 2 | `pytest tests/ -q` all pass | **PARTIAL** | **62 passed, 1 skipped** (`test_qa001_outlier_high_price` in `test_aggregator.py`) |
| 3 | Latest data: ≥3 community sources, ≥15 products in `normalized_observations` | **PASS** | **5** distinct community sources, **22** distinct products, **193** observation rows |
| 4 | `output/public/public_report.json` exists before QA | **FAIL** (initially) | Directory **missing** until Team 50 ran publish (see T02) |

---

## Environment

- **Host:** macOS, Python **3.11.15**, `DATABASE_URL` from repo `.env` (not pasted).  
- **Python path:** `/opt/homebrew/bin/python3.11` (`.venv/bin/python` not used; equivalent interpreter).

---

## T01 — Unit tests (`test_aggregator.py`, `test_publisher_local.py`)

**Command:**

```bash
python3.11 -m pytest tests/test_aggregator.py tests/test_publisher_local.py -v
```

**Output (abridged):**

```text
collected 15 items
... 14 passed, 1 skipped ...
tests/test_aggregator.py::test_qa001_outlier_high_price SKIPPED
```

**Counts:** 8 aggregator tests collected (1 skipped), 6 publisher tests passed → **14 passed** meets minimum **14** executed passes; **mandate “Zero failures”** is met, but **1 SKIPPED** violates strict “all pass” / onboarding “no skip unless documented.”

**Result:** **CONDITIONAL / FAIL (strict)** — recommend **Team 10** remove skip or document waiver (**Team 100**).

---

## T02 — End-to-end pipeline run

**Mandate commands:** `run_ingestion --normalize`, `run_normalizer --metrics`, `run_aggregator`, `run_publisher`.

**Finding:** `organic_market_agent/__main__.py` exposes **`run_ingestion`**, **`run_normalizer`**, **`run_viewer`**, **`run_admin`** only — **no** `run_aggregator`, **`run_publisher`**, or **`run_normalizer --metrics`**.

**Team 50 equivalent (evidence):**

1. `AggregatorEngine().run(session, aggregate_date=2026-03-30)`  
2. `PublishEngine().run(session, Path("output/public"), report_date=2026-03-30)`

**CLI log (abridged):**

```text
AggregatorEngine: date=2026-03-30 daily_groups=22 created=22 updated=0
PublishEngine: wrote 19 products to .../output/public
```

**Post-checks:**

| Artifact / query | Result |
|------------------|--------|
| `SELECT COUNT(*) FROM daily_aggregates` | **22** (> 0) |
| `SELECT COUNT(*) FROM weekly_snapshots` | **22** (> 0) |
| `output/public/public_report.json` | **exists**, valid JSON |
| `output/public/manifest.json` | **exists**, valid JSON |
| `output/public/public_report.html` | **exists** |

**Result:** **CONDITIONAL PASS** on **outcomes**; **FAIL** on **mandate command fidelity** — **Team 10** should add CLIs or update mandate to match shipped entry points.

---

## T03 — Publish threshold enforcement

- **DB (`aggregate_date = CURRENT_DATE`):** **19** rows `meets_publish_threshold = true`, **3** false (`PRD003`, `PRD015`, `PRD016`).  
- **`public_report.json`:** **19** products; **none** of the below-threshold codes appear in JSON.

**Result:** **PASS**

---

## T04 — JSON schema validation

Mandate Python assertion block executed on `output/public/public_report.json` — **no AssertionError**; **19** products, all required fields present.

**Result:** **PASS**

---

## T05 — Aggregation consistency

**Query 1 (min ≤ median ≤ max):**

```text
violations: 0
```

**PASS**

**Query 2 (sample_size vs join count):**

```text
mismatch rows: 1
(78, 19, 24)   -- id=78, sample_size=19, actual_count=24
```

**Result:** **FAIL** (mandate: **0 rows**)

---

## T06 — Staleness calculation

- **`manifest.json`:** `staleness_level`: **`current`** for `last_published_at` on report date.  
- **Unit tests** in `test_publisher_local.py` cover **current / warning / irrelevant** (`test_manifest_staleness_*`) — **PASS** in T01.

**Result:** **PASS** (combined manifest spot-check + T01 coverage)

---

## T07 — Local viewer

**Mandate** shows `run_viewer --port 8082` **without** `--dir`; **actual** CLI **requires** `--dir` (publish output path).

**Executed:**

```bash
python3.11 -m organic_market_agent run_viewer --host 127.0.0.1 --port 8082 --dir output/public
curl http://127.0.0.1:8082/manifest.json
curl http://127.0.0.1:8082/public_report.json
```

**HTTP 200**, JSON parses (e.g. `manifest.json` shows `staleness_level`, `product_count`).

**Result:** **PASS** (with **mandate doc fix** for `--dir`)

---

## T08 — Admin monitoring dashboard

**Executed:**

```bash
python3.11 -m organic_market_agent run_admin --host 127.0.0.1 --port 8083
```

**HTTP status (urllib):** `/` **200**, `/sources` **200**, `/sources/SRC002` **200**, `/products` **200**, `/unresolved` **200**.

**Manual content:** Not deep-reviewed in this pass beyond successful HTML responses; **spot-check** only.

**Result:** **PASS** (HTTP); **manual table accuracy** → recommend **sampling** on next cycle.

---

## T09 — Regression M1–M3 tables

**Counts after M4 aggregate + publish (this DB):**

```text
sources                 20
products                29
product_aliases (active) 97
normalized_observations  193
raw_extracted_items      2458
```

**Issue:** Mandate requires **pre-M4 baseline** captured **before** M4 run. Team 50 **did not** have an authenticated quiet-DB snapshot at session open for **`raw_extracted_items`** / **`product_aliases`**; cannot prove **unchanged** vs pre-M4.

**Result:** **FAIL** (evidence incomplete) — re-run with **T09 quiet-DB protocol** (see Team 10 G3/T09 protocol pattern).

---

## Full test suite (mandate §3 checklist)

```text
62 passed, 1 skipped in 0.65s
```

**Result:** **FAIL** if “all pass” implies **zero skips**.

---

## Scoring table (mandate §3)

| ID | Criterion | Result |
|----|-----------|--------|
| T01 | Aggregator + publisher unit tests | **FAIL** (1 skip) / borderline |
| T02 | Full pipeline + artifacts | **PARTIAL** (outcomes OK, CLI gap) |
| T03 | Threshold JSON vs DB | **PASS** |
| T04 | JSON schema | **PASS** |
| T05 | Consistency | **FAIL** |
| T06 | Staleness | **PASS** |
| T07 | Local viewer | **PASS** |
| T08 | Admin routes | **PASS** |
| T09 | Regression | **FAIL** (no baseline) |
| — | 48+ tests, no regressions | **FAIL** (1 skip) |

---

## Critical findings

1. **T05:** `daily_aggregates.sample_size` **does not match** observation-count join logic in mandate SQL for **at least** `id=78` — **Owner: Team 10** (aggregator SQL vs `sample_size` definition).  
2. **T02:** Missing **`run_paggregator` / `run_publisher`** (and **`--metrics`**) on package CLI — **Owner: Team 10** + **Team 100** (mandate vs implementation).  
3. **T01 / suite:** **Skipped** outlier test — **Owner: Team 10**.  
4. **T09:** Baseline not captured — **Owner: Team 50** re-run with protocol + **Team 10** quiet DB.

---

## Required actions to open Gate G4

- [ ] Fix **T05** join / `sample_size` semantics — **Team 10**  
- [ ] Add or document **CLI** for aggregator + publisher — **Team 10 / Team 100**  
- [ ] Resolve **pytest skip** — **Team 10**  
- [ ] Re-run **T09** with **documented pre/post** counts — **Team 50**  
- [ ] Update **QA_MANDATE_G4.md** `run_viewer` example to include **`--dir`** — **Team 100**

---

## Forwarding

Per mandate §5: **Do not** forward to Team 100 for architectural sign-off until **PASS** or approved **CONDITIONAL** with listed closures.

**Gate G4:** **BLOCKED**
